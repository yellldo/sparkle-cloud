spring:
  application:
    name: sparkle-register
  profiles:
    active: dev
logging:
  level:
    com.sparkle: info
server:
  undertow:
    buffer-size: 1024
    direct-buffers: on
    always-set-keep-alive: true